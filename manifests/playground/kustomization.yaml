apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - helmrepo.yaml
configMapGenerator:
  - name: config
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
